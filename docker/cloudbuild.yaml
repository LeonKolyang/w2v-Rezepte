steps:
- name: 'gcr.io/cloud-builders/docker'
  dir:  'workspace/'
  args: ['build', '-f', 'Dockerfile', '.']
- name: 'gcr.io/cloud-builders/gcloud'
  dir:  'workspace/'
  args: ['app', 'deploy', '--ignore-file=Dockerfile']

